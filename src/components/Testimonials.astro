---
export interface Props {
  title?: string;
  subtitle?: string;
  testimonials?: Array<{
    quote: string;
    name: string;
    position: string;
    company: string;
    image?: string;
    results?: {
      metric: string;
      improvement: string;
    };
    featured?: boolean;
  }>;
}

const {
  title = "So fühlt sich KI im Alltag unserer Kunden an",
  subtitle = "Hier zeigen Ihnen unsere Kunden, welche Prozesse sie mit unseren KI Agenten entlastet haben, welche Ergebnisse sie sehen und wie schnell die Lösungen im Alltag angekommen sind.",
  testimonials = [
    {
      quote:
        "Als HR-Team von zwei Personen waren wir mit Urlaubsanträgen und Onboarding-Dokumenten überlastet. Mit dem KI-System für HR-Prozesse sparen wir jetzt 12 Stunden pro Woche.",
      name: "Lisa Hoffmann",
      position: "HR-Leiterin",
      company: "Mittelständischer Maschinenbau (80 Mitarbeiter)",
      image: "https://randomuser.me/api/portraits/women/45.jpg",
      results: {
        metric: "60%",
        improvement: "Weniger Dateneingabe",
      },
      featured: true,
    },
    {
      quote:
        "Unser Support-Team kann sich endlich auf komplexe Fälle konzentrieren. Der KI-Ticketing-Agent beantwortet 70% der Standard-Anfragen automatisch in unter 2 Minuten.",
      name: "Dr. Michael Weber",
      position: "CTO",
      company: "Software-Unternehmen (120 Mitarbeiter)",
      image: "https://randomuser.me/api/portraits/men/32.jpg",
      results: {
        metric: "70%",
        improvement: "Tickets automatisiert",
      },
    },
    {
      quote:
        "Von 800 Rechnungen im Monat verarbeitet unser KI-System 650 vollautomatisch mit OCR-Technologie. Die Fehlerquote liegt unter 2% und die Durchlaufzeit ist 45% schneller.",
      name: "Thomas Schneider",
      position: "Leiter Operations",
      company: "Logistikunternehmen (250 Mitarbeiter)",
      image: "https://randomuser.me/api/portraits/men/28.jpg",
      results: {
        metric: "45%",
        improvement: "Schnellere Durchlaufzeit",
      },
    },
    {
      quote:
        "Nach 6 Monaten und €85.000 hatten wir nur frustrierte Mitarbeiter und kein funktionierendes System. Codariq hat das gescheiterte Projekt analysiert, gerettet und in 3 Monaten produktiv gemacht.",
      name: "Petra Richter",
      position: "Finanzleiterin",
      company: "Familienunternehmen (180 Mitarbeiter)",
      image: "https://randomuser.me/api/portraits/women/52.jpg",
      results: {
        metric: "8 Monate",
        improvement: "ROI erreicht",
      },
    },
    {
      quote:
        "Neue Mitarbeiter finden Antworten sofort in unserem internen KI-Wissensmanagement-Assistenten. Er kennt 4 Jahre Dokumentation auswendig und reduziert wiederholte Fragen um 85%.",
      name: "Sarah Müller",
      position: "Leiterin Kundenservice",
      company: "E-Commerce (95 Mitarbeiter)",
      image: "https://randomuser.me/api/portraits/women/38.jpg",
      results: {
        metric: "85%",
        improvement: "Weniger Rückfragen",
      },
    },
    {
      quote:
        "KI hat unsere Arbeitsweise über 5 Abteilungen hinweg transformiert. Von HR über Einkauf bis Controlling – überall sehen wir messbare Verbesserungen von durchschnittlich 30% Effizienzsteigerung.",
      name: "Dr. Anna Schmidt",
      position: "Geschäftsführerin",
      company: "Produktionsunternehmen (400 Mitarbeiter)",
      image: "https://randomuser.me/api/portraits/women/44.jpg",
      results: {
        metric: "30%",
        improvement: "Effizienzgewinn",
      },
    },
  ],
} = Astro.props;
---

<section id="testimonials" class="py-12 sm:py-16 md:py-20 bg-white">
  <div class="max-w-8xl mx-auto px-3 sm:px-4 md:px-6 lg:px-12 xl:px-16">
    <!-- Section Header -->
    <div class="text-center mb-10 sm:mb-12 md:mb-16 px-4">
      <h2
        class="text-2xl sm:text-3xl md:text-4xl font-black text-gray-900 mb-3 sm:mb-4 animate-on-scroll"
      >
        {title}
      </h2>
      <p
        class="text-base sm:text-lg md:text-xl font-light text-gray-600 max-w-3xl mx-auto leading-relaxed animate-on-scroll"
        style="transition-delay: 0.15s"
      >
        {subtitle}
      </p>
    </div>

    <!-- Testimonials Grid -->
    <div
      class="grid grid-cols-1 md:grid-cols-2 gap-3 sm:gap-4 md:gap-6 max-w-8xl mx-auto px-2 sm:px-3 md:px-4 relative"
    >
      {
        testimonials.map((testimonial, index) => {
          // Progressive gradient classes based on row
          const getGradientClass = (idx) => {
            if (idx < 2) return "testimonial-gradient-light";   // Row 1: cards 1-2
            if (idx < 4) return "testimonial-gradient-medium";  // Row 2: cards 3-4
            return "testimonial-gradient-heavy";                // Row 3: cards 5-6
          };
          const gradientClass = getGradientClass(index);
          const isHiddenOnMobile = index >= 2; // Hide testimonials 3-6 on mobile
          return (
            <div
              class={`group animate-fade-in-up relative ${gradientClass} ${isHiddenOnMobile ? "hidden md:block" : ""}`}
              style={`animation-delay: ${(index + 1) * 150}ms`}
            >
              {/* Testimonial Card */}
              <div
                class={`
              rounded-xl p-4 sm:p-5 md:p-6 shadow-sm border h-full flex flex-col transition-all duration-300
              bg-white border-gray-200 hover:shadow-md
            `}
              >
                {/* Quote Icon */}
                <div class="mb-3 sm:mb-4">
                  <svg
                    class="w-5 h-5 sm:w-6 sm:h-6 text-gray-300"
                    fill="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path d="M14.017 21v-7.391c0-5.704 3.731-9.57 8.983-10.609l.995 2.151c-2.432.917-3.995 3.638-3.995 5.849h4v10h-9.983zm-14.017 0v-7.391c0-5.704 3.748-9.57 9-10.609l.996 2.151c-2.433.917-3.996 3.638-3.996 5.849h3.983v10h-9.983z" />
                  </svg>
                </div>

                {/* Quote Text */}
                <blockquote class="mb-4 sm:mb-5 leading-relaxed text-sm sm:text-base flex-1 text-gray-700">
                  "{testimonial.quote}"
                </blockquote>

                {/* Author Info */}
                <div class="flex items-center gap-3 sm:gap-4">
                  {/* Avatar */}
                  <div class="flex-shrink-0">
                    <div class="rounded-full overflow-hidden border-2 w-12 h-12 sm:w-14 sm:h-14 border-gray-200">
                      {testimonial.image ? (
                        <img
                          src={testimonial.image}
                          alt={`${testimonial.name}, ${testimonial.position} bei ${testimonial.company}`}
                          class="w-full h-full object-cover"
                          loading="lazy"
                          decoding="async"
                        />
                      ) : (
                        <div class="w-full h-full flex items-center justify-center text-white font-bold text-sm bg-gray-400">
                          {testimonial.name
                            .split(" ")
                            .map((n) => n[0])
                            .join("")}
                        </div>
                      )}
                    </div>
                  </div>

                  <div class="flex-1 min-w-0">
                    {/* Author Details */}
                    <div class="mb-2">
                      <div class="font-bold text-sm sm:text-base text-gray-900">
                        {testimonial.name}
                      </div>
                      <div class="text-xs sm:text-sm text-gray-600">
                        {testimonial.position}
                      </div>
                      <div class="text-xs sm:text-sm font-medium text-gray-500">
                        {testimonial.company}
                      </div>
                    </div>
                  </div>

                  {/* Results Badge */}
                  {testimonial.results && (
                    <div class="text-center px-3 py-2 rounded-lg border flex-shrink-0 border-gray-200 bg-gray-50">
                      <div class="font-bold text-lg sm:text-xl text-gray-900">
                        {testimonial.results.metric}
                      </div>
                      <div class="text-xs font-medium text-gray-600">
                        {testimonial.results.improvement}
                      </div>
                    </div>
                  )}
                </div>

                {/* Decorative Element */}
                <div class="absolute bottom-4 right-4 opacity-5 transition-opacity duration-300">
                  <svg
                    class="w-8 h-8 sm:w-12 sm:h-12 text-gray-500"
                    fill="currentColor"
                    viewBox="0 0 24 24"
                  >
                    <path d="M12 2L2 7L12 12L22 7L12 2Z" />
                  </svg>
                </div>
              </div>
            </div>
          );
        })
      }

      <!-- Gradient Overlay for Second Row -->
      <div class="absolute inset-0 pointer-events-none">
        <div
          class="absolute top-1/2 left-0 right-0 bottom-0 bg-gradient-to-t from-gray-50 via-gray-50/60 to-transparent"
        >
        </div>
      </div>
    </div>

    <!-- Call to Action -->
    <div class="text-center -mt-16 sm:-mt-20 md:-mt-24 relative z-20 px-4">
      <div
        class="bg-white rounded-xl p-6 sm:p-8 shadow-sm border border-gray-200 max-w-4xl mx-auto animate-on-scroll"
      >
        <h3
          class="text-xl sm:text-2xl font-black text-gray-900 mb-3 sm:mb-4 animate-on-scroll"
        >
          Überzeugt? Lassen Sie uns sprechen!
        </h3>
        <p
          class="text-sm sm:text-base font-light text-gray-600 mb-4 sm:mb-6 max-w-3xl leading-relaxed animate-on-scroll"
          style="transition-delay: 0.15s"
        >
         In 30 Minuten schauen wir gemeinsam auf Ihren Arbeitsalltag und finden ein bis zwei konkrete Stellen, an denen KI Automatisierung Ihnen persönlich Zeit, Nerven und Routinearbeit abnimmt.
        </p>
        <div class="animate-on-scroll" style="transition-delay: 0.3s">
          <a
            href="#final-cta"
            class="inline-flex items-center gap-2 bg-gradient-to-br from-orange-500 to-orange-700 text-white px-4 sm:px-6 py-2 sm:py-3 rounded-full hover:from-orange-600 hover:to-orange-800 transition-all duration-200 font-medium text-sm sm:text-base focus:outline-none focus:ring-4 focus:ring-orange-200 shadow-lg cta-button"
          >
            Kostenloses Strategiegespräch
            <svg
              class="w-4 h-4"
              fill="none"
              stroke="currentColor"
              viewBox="0 0 24 24"
            >
              <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </a>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Light gradient for Row 1 (cards 1-2) - Slight overlay */
  .testimonial-gradient-light {
    position: relative;
  }

  .testimonial-gradient-light::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      rgba(249, 250, 251, 0.2) 30%,
      rgba(249, 250, 251, 0.4) 60%,
      rgba(249, 250, 251, 0.6) 100%
    );
    pointer-events: none;
    z-index: 1;
  }

  /* Medium gradient for Row 2 (cards 3-4) - Medium overlay */
  .testimonial-gradient-medium {
    position: relative;
  }

  .testimonial-gradient-medium::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      rgba(249, 250, 251, 0.4) 20%,
      rgba(249, 250, 251, 0.65) 50%,
      rgba(249, 250, 251, 0.85) 100%
    );
    pointer-events: none;
    z-index: 1;
  }

  /* Heavy gradient for Row 3 (cards 5-6) - Heavy overlay */
  .testimonial-gradient-heavy {
    position: relative;
  }

  .testimonial-gradient-heavy::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(
      to bottom,
      transparent 0%,
      rgba(249, 250, 251, 0.6) 15%,
      rgba(249, 250, 251, 0.85) 35%,
      rgba(249, 250, 251, 1) 100%
    );
    pointer-events: none;
    z-index: 1;
  }
</style>
